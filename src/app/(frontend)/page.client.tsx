'use client'

import React, { useEffect } from 'react'
import type { FC } from 'react'
// import { useHeaderTheme } from '@/providers/HeaderTheme'

const HomePageClient: FC = () => {
  // const { setHeaderTheme } = useHeaderTheme()

  // // Set the header theme to dark since we have a dark hero section
  // useEffect(() => {
  //   setHeaderTheme('dark')
    
  //   // Return to default theme when component unmounts
  //   return () => {
  //     setHeaderTheme('light')
  //   }
  // }, [setHeaderTheme])

  // Add any client-side interactivity here
  return (
    <>
      {/* Client-side only components can go here */}
    </>
  )
}

export default HomePageClient
